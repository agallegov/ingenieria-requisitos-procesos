# Herramienta de ingeniería de requisitos

Lee `AGENTS.md` y después `RUNBOOK.md` entero, y sigue el runbook al pie de
la letra.

Regla dura: nunca escribas nada dentro de esta carpeta (es una herramienta
compartida y reutilizable). El proyecto del usuario se crea y vive FUERA, en
su carpeta de trabajo; si la sesión corre aquí dentro, pregunta dónde quiere
guardarlo.
